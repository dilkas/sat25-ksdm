a = {1,...,3}
b = {1,...,3}

U(a)
B(a, b)

U(x) v B(x, y).

forall x exist y !B(y, x).
!B(y, x) ^ !B(z, x) => y=z.

exist x forall y B(x, y).
forall x forall z (forall y B(x, y)) ^ (forall y B(z, y)) => x=z.
